import { SpecDirective } from './spec.directive';

describe('SpecDirective', () => {
  it('should create an instance', () => {
    const directive = new SpecDirective();
    expect(directive).toBeTruthy();
  });
});
